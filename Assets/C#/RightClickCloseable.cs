using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// 挂在关闭按钮上，标记该按钮可被右键触发。
/// RightClickBackManager 会自动找到最上层激活的按钮并触发。
/// </summary>
[RequireComponent(typeof(Button))]
public class RightClickCloseable : MonoBehaviour
{
    // 无需任何代码，仅作标记
}
